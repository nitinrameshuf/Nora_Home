# Nora Home — core platform requirements.
# Target: Python 3.11 - 3.13 (Raspberry Pi OS Trixie ships 3.13).

# --- Web ---
Django>=5.2,<6.0                # LTS, supported until April 2028
djangorestframework>=3.15
django-filter>=24.3
whitenoise>=6.7                 # static files without nginx in front of gunicorn

# --- Realtime (display bus / kiosk control / Nora bot pushes) ---
channels>=4.1
channels-redis>=4.2
daphne>=4.1

# --- Async work ---
celery>=5.4
django-celery-beat>=2.7         # schedules editable in the admin
django-celery-results>=2.5
redis>=5.0
kombu>=5.4                      # AMQP transport for RabbitMQ

# --- Data stores ---
pymongo>=4.8                    # document store (journals, AI runs, raw payloads)
boto3>=1.35                     # object storage (MinIO / S3)
django-storages>=1.14

# --- AI ---
anthropic>=0.40                 # Claude API
mcp>=1.2                        # Model Context Protocol server

# --- Speech ---
# Hosted TTS (Orpheus), so the Pi needs no local model, GPU or audio toolchain.
# Optional in practice: with NORA_HOME_TTS_PROVIDER=none the house never imports
# it, and nora_home/notifications/tts.py degrades to its stub.
groq>=0.11

# --- Slack (Socket Mode) ---
# The Pi is behind home NAT with a self-signed cert, so Slack can never call in.
# Socket Mode dials out instead; slack_sdk's SocketModeClient needs
# websocket-client for the connection itself.
slack-sdk>=3.33
websocket-client>=1.8

# --- Integrations / utility ---
requests>=2.32
httpx>=0.27
python-dotenv>=1.0
croniter>=3.0
Pillow>=10.4
python-dateutil>=2.9
